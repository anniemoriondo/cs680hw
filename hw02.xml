<?xml version="1.0"?>

<project name="hw02" basedir="." default="run">
    <property environment="env"/>

    <!-- Sources and binaries-->
    <property name="src" location="src"/>
    <property name="bin" location="bin"/>

    <!-- Testing -->
    <property name="test.src" location="test/src"/>
    <property name="test.bin" location="test/bin"/>

    <path id="classpath">
        <pathelement location="bin"/>
        <pathelement location="test/bin"/>
        <!-- JUnit libraries-->
        <fileset dir="${env.JUNIT}" includes="*junit*.jar" excludes="*vintage*.jar"/>
        <fileset dir="${env.JUNIT}" includes="*opentest4j*.jar" />
        <fileset dir="${env.JUNIT}" includes="*apiguardian*.jar"/>
    </path>

    <target name="init">
        <mkdir dir="${bin}" />
        <mkdir dir="${test.bin}" />
    </target>

    <target name="compile" depends="init">
        <javac srcdir="${src}" destdir="${bin}" includeAntRuntime="no">
            <classpath refid="classpath" />
            <include name="edu/umb/cs680/hw02/*.java"/>
        </javac>
    </target>

    <target name="run" depends="compile">
        <java classname="edu.umb.cs680.hw02.*"
              classpathref="classpath" fork="true"/>
    </target>

</project>